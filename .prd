[
  {
    "id": "process-page-epochs",
    "subject": "Process all 1595 extracted PDF pages in 10-page epochs to extract cards",
    "status": "pending",
    "description": "Execute batch processor: node scripts/run_epoch_extraction.mjs. This processes all 160 epochs (1595 pages total) in sequence. Each epoch: load 10 page images, invoke opencode CLI to extract cards, save JSON to data/extracted_cards/, update checkpoint. Resumable: interrupt and re-run to continue from last completed epoch.",
    "blocking": ["create-flashcard-index"],
    "blockedBy": [],
    "effort": "large",
    "category": "feature",
    "acceptance": [
      "All 1595 pages processed in 10-page batches",
      "Each batch produces extracted cards in JSON",
      "Cards cross-referenced with codebase",
      "Progress checkpointed (resumable if interrupted)",
      "Total card count documented",
      "Sample cards verified for quality"
    ],
    "edge_cases": [
      "AI agent timeouts on large epochs",
      "Duplicate card detection across epochs",
      "Quality variation in extracted content",
      "Memory/resource management across 160+ epochs"
    ]
  },
  {
    "id": "create-flashcard-index",
    "subject": "Create master index of all extracted flashcards",
    "status": "pending",
    "description": "Execute index creator: node scripts/create_index.mjs (after epochs complete). Aggregates all epoch JSON files, deduplicates by question hash, builds topic map and full-text search index. Output: data/cards_index.json (unified database) and data/INDEX.md (documentation with statistics by difficulty and Bloom level).",
    "blocking": [],
    "blockedBy": ["process-page-epochs"],
    "effort": "small",
    "category": "feature",
    "acceptance": [
      "Flashcard index created",
      "Topic-based organization",
      "Searchable by content",
      "Page references intact",
      "SRS metadata present (for future scheduling)",
      "JSON database ready for learning workflow"
    ],
    "edge_cases": [
      "Large number of cards to index (potentially 1000s)",
      "Performance of search/filtering",
      "Metadata consistency across cards"
    ]
  }
]
