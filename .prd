[
  {
    "id": "setup-card-extraction-pipeline",
    "subject": "Set up card extraction pipeline with AI agents processing 10-page epochs",
    "status": "pending",
    "description": "Create infrastructure for processing PDF pages in epochs of 10 pages. Each epoch: (1) load 10 page images, (2) invoke opencode/kilocode AI agent to extract all possible cards, (3) use acpreact to parse and structure cards, (4) cross-reference with codebase search, (5) output to JSON flashcard format. Make resumable with checkpointing.",
    "blocking": ["process-page-epochs"],
    "blockedBy": [],
    "effort": "large",
    "category": "feature",
    "acceptance": [
      "Card extraction pipeline created",
      "Epoch processing logic (10 pages per batch)",
      "AI agent integration (opencode/kilocode)",
      "acpreact extraction working",
      "Codebase search integration available",
      "JSON flashcard output format defined",
      "Checkpointing system for resumability"
    ],
    "edge_cases": [
      "AI agent token limits for large epochs",
      "Handling empty or low-quality extractions",
      "Codebase search availability during processing",
      "JSON file management for thousands of cards"
    ]
  },
  {
    "id": "process-page-epochs",
    "subject": "Process all 933 extracted PDF pages in 10-page epochs to extract cards",
    "status": "pending",
    "description": "Batch process pages 1-933 in epochs of 10 pages. For each epoch: invoke AI agent, extract cards, validate, output to JSON. Start with pages 1-10, then 11-20, etc. Generate flashcards with image references and metadata.",
    "blocking": ["create-flashcard-index"],
    "blockedBy": ["setup-card-extraction-pipeline"],
    "effort": "large",
    "category": "feature",
    "acceptance": [
      "All 933 pages processed in 10-page batches",
      "Each batch produces extracted cards in JSON",
      "Cards cross-referenced with codebase",
      "Progress checkpointed (resumable if interrupted)",
      "Total card count documented",
      "Sample cards verified for quality"
    ],
    "edge_cases": [
      "AI agent timeouts on large epochs",
      "Duplicate card detection across epochs",
      "Quality variation in extracted content",
      "Memory/resource management across 93+ epochs"
    ]
  },
  {
    "id": "create-flashcard-index",
    "subject": "Create master index of all extracted flashcards",
    "status": "pending",
    "description": "Generate INDEX.md and searchable flashcard database from all extracted cards. Include: topic mapping, difficulty levels, source page references, codebase links, card metadata for SRS scheduling.",
    "blocking": [],
    "blockedBy": ["process-page-epochs"],
    "effort": "small",
    "category": "feature",
    "acceptance": [
      "Flashcard index created",
      "Topic-based organization",
      "Searchable by content",
      "Page references intact",
      "SRS metadata present (for future scheduling)",
      "JSON database ready for learning workflow"
    ],
    "edge_cases": [
      "Large number of cards to index (potentially 1000s)",
      "Performance of search/filtering",
      "Metadata consistency across cards"
    ]
  }
]
